apiVersion: apps/v1
kind: Deployment
metadata:
  name: website-deployment
  labels:
    app: argocd-app1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: argocd-app1
  template:
    metadata:
      labels:
        app: argocd-app1
    spec:
      containers:
      - name: argocd-app1-nginy
        image: nginx:alpine
        ports:
        - containerPort: 80
        volumeMounts:
        - name: website-cm-volume
          mountPath: /usr/share/nginx/html
      volumes:
      - name: website-cm-volume 
        configMap:
          name: website-cm

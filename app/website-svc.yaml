apiVersion: v1
kind: Service
metadata:
  labels:
    app: argocd
  name: website-service
spec:
  type: ClusterIP
  selector:
    app: argocd
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
